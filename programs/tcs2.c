#include <stdio.h>

int main(){
    int n, m, r, days = 0;
    scanf("%d %d", &n, &m);
    int friendship[n][n], f_count[n];
    for(int i = 0; i < n; i++){
        for(int j = 0; j < n; j++){
            
        }
    }

    return 0;
}
